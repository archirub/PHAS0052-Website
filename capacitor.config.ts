import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PHAS0052_website',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
